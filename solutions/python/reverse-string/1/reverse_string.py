# Slice is a built in type in python provides instructions where to start, end and order of elemetns. 
# Additionally string is immutable object so using slice here provides a reversed copy.

def reverse(text):
    return text[::-1]